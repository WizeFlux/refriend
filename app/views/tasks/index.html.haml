%h1
  Tasks
  %small= link_to 'New task', new_task_path
  %small= link_to 'My references', references_path

= render 'query_form'
  
    
%hr
- @tasks.each do |task|
  %section{'ng-controller' => 'People'}
    %h3= link_to task.title, task_path(task)
    %p
      %img{src: "{{person(#{task.requester_uid}).photo}}"}
      {{person(#{task.requester_uid}).first_name}} {{person(#{task.requester_uid}).nickname}} {{person(#{task.requester_uid}).last_name}}
      
    %p= task.description
    %p
      - task.tags.each do |tag|
        %i= tag
      
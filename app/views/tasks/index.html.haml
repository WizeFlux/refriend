= render 'tasks/units/query_form'

%hr
.pull-right
  = link_to 'Добавить объявление', new_task_path, class: 'btn btn-primary'
%p.lead Порекоммендуйте друга...
%div{'ng-controller' => 'Static'}
  = paginate @tasks
  - @tasks.each do |task|
    = render 'tasks/units/task', task: task
  = paginate @tasks
      
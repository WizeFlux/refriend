%h1
  Tasks
  %small= link_to 'New task', new_task_path

= render 'query_form'
  
    
%hr
- @tasks.each do |task|
  %section
    %h3= link_to task.title, task_path(task)
    %p= task.description
    %p
      - task.tags.each do |tag|
        %i= tag
